# Imported Testing Standard

- Each behavior change needs a focused pytest.
- Tests must run offline and deterministically.
- Security hooks need both allowed and blocked command cases.

This file is pulled into `CLAUDE.md` with `@standards/testing.md`, demonstrating
how a shared convention can be maintained once and imported where needed.
